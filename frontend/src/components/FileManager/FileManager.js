// FileManager.js
import React from 'react';
import './FileManager.css';

const FileManager = () => {
    return (
        <div className="file-manager">
            <h1>File Manager</h1>
            <p>Manage files and directories on the server</p>
        </div>
    );
};

export default FileManager;
